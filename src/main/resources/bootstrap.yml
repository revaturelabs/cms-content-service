spring:
  cloud:
    config:
      name: content-service
      discovery:
        service-id: config
        enabled: true
        
server:
  servlet: 
    path: /cmsforce/*

---
spring:
  profiles: dev

eureka:   
 client:
   service-url:
     defaultZone: http://localhost:8761/eureka/
 instance:
   prefer-ip-address: true